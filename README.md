# intelligent-gmail-orchestrator
An automated asynchronous message-processing architecture deployed via n8n. Integrates upstream Gmail events with a locally hosted Large Language Model (Ollama) to execute zero-shot text classification, utilizing conditional conditional logic and dynamic error-handling loops to manage contextual system labels dynamically without execution faults.
